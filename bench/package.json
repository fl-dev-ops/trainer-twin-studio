{
  "name": "trainertwin-chunk-bench",
  "private": true,
  "type": "module",
  "scripts": {
    "bench:live": "bun run src/live/runner.ts",
    "bench:local": "bun run src/local/runner.ts",
    "bench": "bun run src/local/runner.ts",
    "local:fetch": "bun run src/local/notion-public-fetch.ts",
    "local:fetch:authenticated": "bun run src/local/notion-fetch.ts",
    "local:golden": "bun run src/local/golden.ts"
  },
  "dependencies": {
    "@chonkiejs/core": "^0.0.11",
    "@chroma-core/default-embed": "^0.1.9",
    "chromadb": "^3.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
